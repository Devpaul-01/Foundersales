// Re-export so pages can import from a dedicated hook path:
//   import { useNotificationContext } from '@/hooks/useNotificationContext';
export { useNotificationContext } from '@/contexts/NotificationContext';
